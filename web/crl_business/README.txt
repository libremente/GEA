For buildind this module:

Using a maven lifecycle, you can:

mvn clean install -> generates the jar file that must be instaled in m2 repository in order to let  crl_web module to connect to Alfresco for all document lifecycle operations.

mvn clean --> cancel jar target file.
